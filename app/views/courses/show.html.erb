<p style="color: green"><%= notice %></p>

<%= render @course %>
<div>
<table class="table">
    <tbody>
            <tr>
            <td> <%= link_to "Edit this course", edit_course_path(@course), class: "btn btn-info btn-lg" %></td>
            <td>  <%= link_to "Back to courses", courses_path , class: "btn btn-secondary btn-lg"%></td>
            <td>  <%= button_to "Destroy this course", @course, method: :delete, class: "btn btn-danger btn-lg" %></td>
           
            </tr>
     
        </tbody>
    </table>
 </div>